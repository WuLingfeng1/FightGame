# v0.1.7 — 四段攻击动作系统

- 日期：2026-06-15
- 时间戳：`[v0.1.7] 2026-06-15 22:30:00`
- Git：`a023e9a` 角色同时新增轻拳,轻腿,重拳,重腿这些攻击动作

## 变更摘要

为 Yagami 和 Orochi 实现完整的四段攻击系统：轻拳(J)、轻腿(K)、重拳(U)、重腿(L)，修复攻击动画的脚部对齐和朝向翻转偏移问题。

## 详细内容

**Yagami 攻击拆分**：StandAttack.png（34帧）拆分为 4 个独立精灵表，每段攻击使用独立文件。帧分配：轻拳 0-5（6帧）、轻腿 6-12（7帧）、重拳 13-22（10帧）、重腿 23-33（11帧）。

**Orochi 攻击配置**：LightKick（13→10帧）、HeavyPunch（16→13帧）、HeavyKick（10帧）精灵表从 384×512 缩放至 251×335，匹配站立帧比例。裁剪 LightKick 和 HeavyPunch 末尾恢复帧，消除回 Stand 时的卡顿。

**脚部对齐修复**：setPosY() 改为按动画状态选择 feetBottom/feetMargin（原来统一用 stand 值）。添加 visualScale 补偿公式 `positionY += (fh - fb) * (vs - 1)`，抵消从 Item 底部缩放导致的脚尖上移。

**朝向翻转补偿**：QML 渲染中 animOffsetX 乘以朝向因子 `(facingLeft ? -1 : 1)`，消除角色换位时的 X 轴跳变。

**P2 按键扩展**：小键盘 1/2/3/0 对应轻拳/轻腿/重拳/重腿。

## 涉及文件

| 文件 | 操作 |
|------|------|
| characterconfig.h | 修改（AnimParams 新增 offsetX；CharacterConfig 新增 lightKick/heavyPunch/heavyKick） |
| characterconfig.cpp | 修改（解析 offsetX、lightKick、heavyPunch、heavyKick） |
| charactermodel.h | 修改（State 枚举新增 LightKick/HeavyPunch/HeavyKick；新增 play 函数声明和成员变量） |
| charactermodel.cpp | 修改（新增 playLightKick/playHeavyPunch/playHeavyKick；configure 加载新参数；setPosY 按状态选择 feetBottom；onTick 处理新状态结束） |
| FightScreen.qml | 修改（P1/P2 按键绑定扩展；animOffsetX 朝向补偿） |
| config/characters.json | 修改（Yagami lightPunch 改用独立精灵表；新增 lightKick/heavyPunch/heavyKick 配置；Orochi 新增三段攻击配置） |
| CMakeLists.txt | 修改（添加新精灵表资源） |
| images/character/Yagami/StandAttack_LightPunch.png | 新建（6帧） |
| images/character/Yagami/StandAttack_LightKick.png | 新建（7帧） |
| images/character/Yagami/StandAttack_HeavyPunch.png | 新建（10帧） |
| images/character/Yagami/StandAttack_HeavyKick.png | 新建（11帧） |
| images/character/Orochi/LightKick.png | 修改（缩放至 251×335，裁剪至 10 帧） |
| images/character/Orochi/HeavyPunch.png | 修改（缩放至 251×335，裁剪至 13 帧） |
| images/character/Orochi/HeavyKick.png | 修改（缩放至 251×335） |
