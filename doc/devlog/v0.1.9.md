# v0.1.9 — 下蹲系统、蹲防机制、攻击特效

- 日期：2026-06-18
- 时间戳：[v0.1.9] 2026-06-18

## 变更摘要

实现下蹲系统（含蹲防无敌机制）和攻击视觉特效系统。Orochi 下蹲冻结 CrouchAttack.png 第0帧，Yagami 下蹲循环播放 Crouch.png。下蹲时攻击键无效（蹲防：被攻击不触发受击动画）。轻拳命中时播放 3 帧攻击特效动画，定位于防御者脖子处。

## 详细内容

**下蹲系统**：CharacterModel 新增 Crouch 和 CrouchAttack 两个状态。P1 按 S、P2 按 Down 进入下蹲。Orochi 使用 CrouchAttack.png 第0帧（冻结），Yagami 使用 Crouch.png 循环播放。下蹲时禁止移动、跳跃、攻击。

**Yagami 下蹲帧图标准化**：原始 Crouch.png（2304×512，6 帧 384×512）通过 Python 脚本 resize 为 1506×335（6 帧 251×335），与 Stand 帧尺寸完全一致，消除运行时缩放导致的卡顿和浮空。

**Orochi 下蹲帧图提取**：从 CrouchAttack.png（10 帧 384×512）提取第0帧 resize 为 251×335 保存为 Crouch.png，与 Stand 帧尺寸一致。

**蹲防机制**：playHurt/Hurt1/Hurt2/Hurt3 开头检查 m_crouching，下蹲时直接 return 不播放受击动画、无击退、无伤害。同时 QML 层所有攻击函数和 stopMove 函数增加 p1Crouching/p2Crouching 守卫，C++ 层 playStand() 增加 m_crouching 守卫，startCrouch 清除 movePressed 标记防止方向键释放打断下蹲。

**攻击特效系统**：FightScreen.qml 新增 attackVfx 组件，3 帧（1.png 15×21 / 2.png 27×23 / 3.png 32×31）每 50ms 依次播放后消失。缩放 fitScale×3.0。X 定位在两角色 30:70 加权中点，Y 定位在防御者视觉脖子处（`positionY + frameHeight × (1 - visualScale × 0.8)`），按角色和攻击类型微调偏移。

**特效偏移量**：Orochi 所有攻击 offset=-30，Yagami 拳类（state 7/9/11）offset=60，Yagami 腿类（state 8/10）offset=120（脚部）。

## 涉及文件

| 文件 | 操作 |
|------|------|
| characterconfig.h | 修改（新增 crouch、crouchAttack 动画参数字段） |
| characterconfig.cpp | 修改（解析 crouch、crouchAttack JSON） |
| charactermodel.h | 修改（State 新增 Crouch、CrouchAttack；新增 playCrouch/playCrouchAttack/stopCrouch） |
| charactermodel.cpp | 修改（下蹲动画、蹲防守卫、setPosY 补偿、onTick 帧处理） |
| fightdirector.h | 不变 |
| fightdirector.cpp | 不变 |
| FightScreen.qml | 修改（S/Down 按键绑定、下蹲输入函数、蹲姿守卫、attackVfx 组件、hitDetected VFX 触发） |
| config/characters.json | 修改（Orochi/Yagami 新增 crouch/crouchAttack 配置） |
| CMakeLists.txt | 修改（新增 Orochi Crouch.png、Attack/1.png 2.png 3.png 资源） |
| images/character/Yagami/Crouch_original.png | 新建（原始 Crouch.png 备份） |
| images/character/Yagami/Crouch.png | 修改（Python resize 2304×512→1506×335） |
| images/character/Orochi/Crouch.png | 新建（从 CrouchAttack.png 提取帧0 resize 251×335） |
| images/Attack/1.png | 已有（攻击特效帧1） |
| images/Attack/2.png | 已有（攻击特效帧2） |
| images/Attack/3.png | 已有（攻击特效帧3） |
